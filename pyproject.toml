[project]
name = "un-votes-intelligence-platform"
version = "0.1.0"
description = "Research-grade UN Votes Analyzer and geopolitical intelligence system"
requires-python = ">=3.11,<3.13"
dependencies = [
    "pandas>=2.2",
    "duckdb>=1.0",
    "pyarrow>=25.0.1",
    "pandera>=0.32.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
